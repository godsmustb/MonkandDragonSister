node_modules/
test/shots/
test/results.json

# Claude Code agent worktrees (temp)
.claude/worktrees/

# FTP deploy secrets (never commit)
deploy.config.json

# Leaderboard runtime data (server-generated)
api/data/scores.json

# Analytics runtime data (server-generated, never commit player data)
api/data/analytics.jsonl

# Unrelated sibling project — never track from this repo
white-feather-finance/
playtest-report.json
